# large binaries — host these, do not version them
# (the catalogue is now a 3.5MB PDF built from the page images already in
#  assets/catalogue/, so it is versioned — the 29MB original was not)
*.mov
*.wav

# build + preview output
artifact_site_preview.html
*.zip

# tooling
node_modules/
package-lock.json
.DS_Store
Thumbs.db
*.log

# editors
.vscode/
.idea/
*.swp

# i18n working files
tools/i18n/*.tar.gz
tools/i18n/chunks/
